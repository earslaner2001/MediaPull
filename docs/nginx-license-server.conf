# MediaPull License Server - Nginx Reverse Proxy Configuration
# This configuration provides HTTPS encryption for the license server

# HTTP to HTTPS Redirect
server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com;  # Replace with your actual domain

    # Redirect all HTTP traffic to HTTPS
    return 301 https://$server_name$request_uri;
}

# HTTPS Server Configuration
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name your-domain.com;  # Replace with your actual domain

    # SSL Certificate Configuration (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # SSL Security Settings (Modern Configuration)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;
    
    # SSL Session Configuration
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;
    
    # OCSP Stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    ssl_trusted_certificate /etc/letsencrypt/live/your-domain.com/chain.pem;
    
    # DNS Resolver (Use your VDS's DNS or public DNS)
    resolver 8.8.8.8 8.8.4.4 valid=300s;
    resolver_timeout 5s;
    
    # Security Headers
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    
    # Logging
    access_log /var/log/nginx/mediapull-license-access.log;
    error_log /var/log/nginx/mediapull-license-error.log;
    
    # Reverse Proxy to License Server
    location / {
        # Proxy to local Node.js license server on port 50000
        proxy_pass http://127.0.0.1:50000;
        
        # Proxy Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # WebSocket Support (if needed for future features)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # Buffer Configuration
        proxy_buffering off;
        proxy_request_buffering off;
    }
    
    # Health Check Endpoint (optional)
    location /health {
        proxy_pass http://127.0.0.1:50000/health;
        access_log off;
    }
    
    # Rate Limiting Zone (Protect against brute force)
    limit_req_zone $binary_remote_addr zone=license_api:10m rate=10r/s;
    limit_req zone=license_api burst=20 nodelay;
    limit_req_status 429;
}

# Installation Instructions:
# 
# 1. Install Nginx on your VDS:
#    sudo apt update
#    sudo apt install nginx
#
# 2. Install Certbot for Let's Encrypt SSL:
#    sudo apt install certbot python3-certbot-nginx
#
# 3. Copy this configuration to Nginx:
#    sudo nano /etc/nginx/sites-available/mediapull-license
#
# 4. Replace 'your-domain.com' with your actual domain in this file
#
# 5. Create symbolic link to enable the site:
#    sudo ln -s /etc/nginx/sites-available/mediapull-license /etc/nginx/sites-enabled/
#
# 6. Test Nginx configuration:
#    sudo nginx -t
#
# 7. Obtain Let's Encrypt SSL certificate:
#    sudo certbot --nginx -d your-domain.com
#
# 8. Restart Nginx:
#    sudo systemctl restart nginx
#
# 9. Enable Nginx to start on boot:
#    sudo systemctl enable nginx
#
# 10. Configure firewall to allow HTTPS:
#     sudo ufw allow 'Nginx Full'
#     sudo ufw enable
#
# 11. Update your application to use HTTPS URL:
#     Change LICENSE_SERVER_URL to https://your-domain.com
#
# Note: Certbot will automatically renew certificates. Verify auto-renewal:
#       sudo certbot renew --dry-run
